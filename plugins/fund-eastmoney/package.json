{
  "name": "@conanchen/fund-eastmoney",
  "version": "0.1.0",
  "description": "Tauri Plugin for Sentry",
  "license": "Apache-2.0 OR MIT",
  "files": [
    "webview-dist"
  ],
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "pnpm build:ts && pnpm build:rust",
    "build:ts": "tsc --project ./js/tsconfig.json && rollup -c ./js/rollup.config.js",
    "build:rust": "cargo build",
    "example": "pnpm build && cd examples/basic-app && pnpm install && pnpm tauri dev",
    "prepublishOnly": "pnpm build"
  },
  "devDependencies": {
    "@rollup/plugin-commonjs": "^25.0.3",
    "@rollup/plugin-json": "^6.0.0",
    "@rollup/plugin-node-resolve": "13.3.0",
    "@rollup/plugin-typescript": "^11.1.2",
    "rollup": "2.75.7",
    "rollup-plugin-terser": "7.0.2",
    "typescript": "^5.1.6"
  },
  "dependencies": {
    "@tauri-apps/api": "^1.4.0",
    "cash-dom": "^8.1.5",
    "tslib": "^2.6.0"
  }
}